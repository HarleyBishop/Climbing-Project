// Key names used when reading/writing tokens to localStorage.
// Centralised here so a rename only happens in one place rather than being
// scattered across every component that touches auth.
export const ACCESS_TOKEN = "access"
export const REFRESH_TOKEN = "refresh"
